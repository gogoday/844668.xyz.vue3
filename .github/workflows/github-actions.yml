name: vue ci
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀

env:
  PIGDIG_KEY: ${{ secrets.PIGDIG_KEY }}
  # 定义输出目录，方便统一维护
  OUTPUT_DIR: dist
  DEPLOY_DIR: /data/844668.xyz
  DIST_TAR_FILE: 844668.vue3.tar


on:
  push:
    branches: [ $default-branch ]
  pull_request:
    branches: [ $default-branch ]
# jobs:
#   Explore-GitHub-Actions:
#     runs-on: ubuntu-latest
#     steps:
#       - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
#       - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
#       - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
#       - name: Check out repository code
#         uses: actions/checkout@v5
#       - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
#       - run: echo "🖥️ The workflow is now ready to test your code on the runner."
#       - name: List files in the repository
#         run: |
#           ls ${{ github.workspace }}
#       - name: npm i
#       - run: 
#       - run: echo "🍏 This job's status is ${{ job.status }}."

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [24.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - uses: actions/checkout@v6
      uses: actions/setup-node@v6
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    - run: npm i -g pnpm@10
    - run: pnpm i
    - run: pnpm build
    - run: cd dist && tar cvf ${{ DIST_TAR_FILE }} ./* && cd ..
    - name: Publish to server
      uses: gogoday/ssh-scp-deploy@master
      with:
        src: ./dist/${{ DIST_TAR_FILE }}
        host: pig-dig.cn
        remote: /data/
        port: 22
        user: root
        key: ${{ env.PIGDIG_KEY }}
        # 优化部署命令：先备份、清空目标目录、解压、重启并检查服务状态
        cmd: |
          cd /data/ && \
          echo "=== Current directory files ===" && \
          ls -la && \
          # 创建目标目录（确保存在）
          mkdir -p ${{ env.DEPLOY_DIR }} && \
          # 解压新产物（覆盖旧文件）
          tar xzvf ${{ DIST_TAR_FILE }} -C ${{ env.DEPLOY_DIR }} && \
          echo "=== Unpack completed ===" && \
          ls -la ${{ env.DEPLOY_DIR }}